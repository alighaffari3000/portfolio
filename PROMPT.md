# PROMPT — تحویل به توسعه‌دهنده

> متن زیر را عیناً به همکار (یا به یک ایجنت کدنویسی) بدهید.

---

## نقش و هدف

تو مسئول پیاده‌سازی کامل یک وب‌سایت **Portfolio + Project Case Studies** هستی.

خروجی نهایی صرفاً یک پورتفولیوی معمولی با کارت پروژه نیست؛ هر پروژه باید یک **صفحهٔ کامل و
قابل ارائه** با Route مستقل داشته باشد، و تمام محتوا از طریق یک **Admin Panel** در دیتابیس
مدیریت شود — طوری که افزودن پروژهٔ جدید **هیچ تغییری در کد** و **هیچ Rebuildی** لازم نداشته باشد.

میزبانی روی یک **VPS اوبونتو** است (نه هاست استاتیک)، پس فایل‌سیستم پایدار و پروسهٔ Node دائمی داری.

## اسناد

دو فایل در ریشهٔ پروژه هست. **قبل از نوشتن حتی یک خط کد، هر دو را کامل بخوان:**

- **`PLAN.md`** — منبع حقیقت معماری. جزئیات schema، auth، upload، امنیت، استقرار و
  چک‌لیست ۲۴ موردی verification در آن است.
- **`TASKS.md`** — ترتیب اجرا. ۱۴ تسک در ۵ فاز، هرکدام با معیار پذیرش مشخص.

در صورت هر تعارضی، **`PLAN.md` ارجح است**. اگر جایی از `PLAN.md` مبهم بود، **بپرس** —
حدس نزن و خودت تصمیم معماری نگیر.

## Stack

```
Astro · React · TypeScript · Tailwind CSS
@astrojs/node (SSR / standalone)
Prisma · SQLite
bcrypt
PM2 · Nginx · Certbot
```

پایهٔ ظاهری از ریپوی [`Gothsec/Astro-portfolio`](https://github.com/Gothsec/Astro-portfolio) گرفته می‌شود.

## ممنوعیت‌ها

اینها را **اضافه نکن**، حتی اگر به نظرت بهتر می‌آیند:

```
Redis · PostgreSQL · Docker · بک‌اند یا سرویس API جدا
CMS · MDX · Rich Text Editor
جدول User · جدول Session
busboy (مگر ثابت کنی FormData بومی کافی نیست)
هر Abstraction، لایه، یا وابستگی‌ای که PLAN.md نخواسته
```

## قواعد غیرقابل‌مذاکره

۱. **صفحات public مستقیماً Prisma را صدا می‌زنند.**
   مسیر درست: `Browser → /projects/foo → Prisma → HTML`
   مسیر ممنوع: `Browser → /projects/foo → /api/... → Prisma`
   API فقط برای عملیات نوشتن/حذف/آپلود ادمین وجود دارد.

۲. **تفاوت redirect و 401.** مسیرهای `/admin/*` در حالت unauthenticated به صفحهٔ لاگین
   `302` می‌شوند؛ مسیرهای `/api/admin/*` باید `401` با بدنهٔ **JSON** برگردانند.
   هرگز کلاینت API را به HTML redirect نکن.

۳. **دایرکتوری آپلود بر اساس `project id` است، نه `slug`** — تا تغییر slug فایل‌ها را نشکند.

۴. **در دیتابیس URL عمومی ذخیره می‌شود، نه مسیر فایل‌سیستم:**
   `/uploads/projects/<project-id>/hero.webp`

۵. **`/uploads/` را Nginx مستقیماً سرو می‌کند** — تصاویر از داخل Astro proxy نمی‌شوند.

۶. **هرگز HTML دلخواه کاربر رندر نکن.** بدون `set:html` روی محتوای ورودی. برای فیلدهای
   Case Study در پنل ادمین از `textarea` ساده استفاده کن و line breakها را امن حفظ کن.

۷. **همهٔ بخش‌های صفحهٔ Case Study اختیاری‌اند** و فقط در صورت غیرخالی بودن رندر می‌شوند.

۸. **در production فقط `prisma migrate deploy`** — هرگز `migrate dev`.

۹. **فایل orphan باقی نماند:** جایگزینی hero فایل قبلی را حذف کند؛ حذف تصویر گالری فایل
   فیزیکی را هم حذف کند؛ حذف پروژه کل دایرکتوری آپلودش را پاک کند.

۱۰. **دیتابیس و uploads خارج از ریپو** بمانند: `/var/lib/portfolio/`

۱۱. احراز هویت روی **هر** mutation ادمین بررسی شود. Secrets داخل Git نروند.

## روش کار

- تسک‌های `TASKS.md` را **به ترتیب** جلو ببر (فاز ۰ → ۴). فازها را جابه‌جا نکن؛ مثلاً
  API را قبل از لایهٔ auth و upload ننویس.
- در پایان **هر فاز** گزارش بده: چه ساخته شد، چه تصمیمی گرفتی، چه چیزی باز مانده.
- اگر تسکی طبق `PLAN.md` قابل انجام نبود، **متوقف شو و بپرس** — دور نزن.
- کد را در همان سبک پروژهٔ پایه بنویس (نام‌گذاری، ساختار پوشه، تراکم کامنت).

## شرط اتمام کار

کار وقتی تمام است که:

- هر ۱۴ تسک `TASKS.md` معیار پذیرش خودش را پاس کرده باشد، **و**
- هر ۲۴ مورد چک‌لیست verification در بخش ۱۵ فایل `PLAN.md` **سبز** باشد، **و**
- یک پروژهٔ کامل بتواند صرفاً از طریق Admin Panel — بدون لمس کد یا دیتابیس و بدون Rebuild —
  ساخته، منتشر و روی `/projects/[slug]` دیده شود.

## اصل راهنما

> **Keep the implementation minimal and maintainable. Do not over-engineer.**
> کوچک‌ترین معماری قابل اتکایی که این نیازمندی‌ها را برآورده کند.
