---
import { t, type Locale } from "../lib/i18n";

interface Props {
  locale?: Locale;
}

const { locale = "en" } = Astro.props;
const strings = t(locale);
const why = strings.why;
---

<section class="py-16 md:py-24 text-[var(--white)]">
  <div class="max-w-5xl mx-auto">
    <div class="reveal">
      <p class="text-lg text-[var(--sec)] mb-2 shiny-sec">{why.eyebrow}</p>
      <h2 class="heading-section font-medium mb-8">{why.heading}</h2>
    </div>

    <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
      {
        why.items.map((item) => (
          <li class="reveal flex items-start gap-3 p-4 rounded-2xl border border-[var(--surface-border)] bg-[var(--surface)]">
            <span class="text-[var(--sec)] leading-6">&bull;</span>
            <span class="text-[var(--white-icon)] text-sm md:text-base">{item}</span>
          </li>
        ))
      }
    </ul>
  </div>
</section>
